<UserControl x:Class="NepseClient.Modules.TradeManagementSystem.Views.TopGainerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DataGrid ItemsSource="{Binding Items}" IsReadOnly="True" AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" />
            <DataGridTextColumn Header="LTP" Binding="{Binding Ltp}" />
            <DataGridTextColumn Header="Point Change" Binding="{Binding PointChange}" />
            <DataGridTextColumn Header="% Change" Binding="{Binding PerChange}" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
