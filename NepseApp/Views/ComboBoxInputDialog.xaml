<Window x:Class="NepseApp.Views.ComboBoxInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NepseApp.Views"
        mc:Ignorable="d"
        Title="ComboBoxInputDialog" Height="94" Width="236">
    <StackPanel>
        <TextBlock Text="{Binding Text}" d:Text="This is the description"/>
        <ComboBox x:Name="ComboItems"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Style="{StaticResource MaterialDesignFlatButton}"/>
            <Button Content="Cancel" Style="{StaticResource MaterialDesignFlatButton}"/>
        </StackPanel>
    </StackPanel>
</Window>
